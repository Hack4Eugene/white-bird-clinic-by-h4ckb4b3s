
//- listing.pug

extends layout.pug 

block contents 
  style
    include ../public/stylesheets/index.css

  article
    section
      if user != null 
        h2 name
      if address != null
        p address
      if phone != null
        p phone
      if hours != null 
        p hours
      if url != null 
        p url
